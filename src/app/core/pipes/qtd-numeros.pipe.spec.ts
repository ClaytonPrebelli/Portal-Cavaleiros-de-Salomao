import { QtdNumerosPipe } from './qtd-numeros.pipe';

describe('QtdNumerosPipe', () => {
  it('create an instance', () => {
    const pipe = new QtdNumerosPipe();
    expect(pipe).toBeTruthy();
  });
});
